spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mypersonalgit15/config-repo
          skipSslValidation: true
server:
  port: 8888

